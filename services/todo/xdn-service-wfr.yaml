---
name: todo_wfr
image: fadhilkurnia/xdn-todo
state: /app/data/
port: 80
consistency: write_follow_reads
deterministic: true

requests:
  - path_prefix: "/"
    methods: "GET,OPTIONS,HEAD"
    behavior: read_only
  - path_prefix: "/"
    methods: "PUT,POST,DELETE"
    behavior: write_only
  - path_prefix: "/"
    methods: "PUT,POST,DELETE"
    behavior: monotonic